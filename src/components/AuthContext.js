import React from 'react';
// This file creates a context for authentication, which can be used to share user data across components.
// It also creates a provider component that can be used to wrap components that need access to the authentication context.




export const AuthContext = React.createContext();

